{
  "name": "discord-bot",
  "version": "1.0.0",
  "description": "My Discord Bot",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",            // 봇 실행
    "deploy": "node deploy-commands.js", // 명령어 등록
    "build": "npm install && npm run deploy" // Render 배포 시 실행
  },
  "dependencies": {
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "sqlite3": "^5.1.6"
  }
}
